import { readInputFile } from "./utils";



export default async (file = './data/dayX.txt', part?: number, ...args: string[]) => {

  await readInputFile(file, ([row], index) => {

  });
  if (!part || part === 1) {


  }
  if (!part || part === 2) {

  }
}